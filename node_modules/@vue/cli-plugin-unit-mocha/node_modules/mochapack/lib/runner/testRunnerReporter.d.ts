/// <reference types="node" />
import EventEmitter from 'events';
declare type ReporterOptions = {
    eventEmitter: EventEmitter;
    interactive: boolean;
    clearTerminal: boolean;
    quiet: boolean;
    cwd: string;
};
export default function testRunnerReporter(options: ReporterOptions): void;
export {};
