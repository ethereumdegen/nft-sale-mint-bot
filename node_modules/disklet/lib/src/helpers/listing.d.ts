import { Disklet, DiskletListing } from '../index';
/**
 * Recursively lists a folder.
 */
export declare function deepList(disklet: Disklet, path?: string): Promise<DiskletListing>;
/**
 * Filters a listing down to just an array of file paths.
 */
export declare function justFiles(listing: DiskletListing): string[];
/**
 * Filters a listing down to just an array of folder paths.
 */
export declare function justFolders(listing: DiskletListing): string[];
